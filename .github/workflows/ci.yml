# =============================================================================
# PacBio HiFi Variant Pipeline — CI (dev/benchmarking)
# =============================================================================
# Manual trigger only — this branch is a benchmarking and validation
# framework intended to be run locally against synthetic test data.
#
# Automated CI is not enabled on this branch because it requires a
# full reference genome that is not committed to the repository.
# Full pipeline validation is performed locally using the provided
# synthetic test data and scripts. See README for instructions.
#
# To run this workflow manually:
#   GitHub → Actions → CI → Run workflow
# =============================================================================

name: CI

on:
  workflow_dispatch:  # manual trigger only

jobs:
  placeholder:
    name: Manual trigger only
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "This branch is validated locally."
          echo "See README for benchmarking instructions."
